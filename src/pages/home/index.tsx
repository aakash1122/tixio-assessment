import React from "react";
import ProductSlider from "../../components/product-slider";

type Props = {};

const Home = (props: Props) => {
  return (
    <div>
      <ProductSlider />
    </div>
  );
};

export default Home;
